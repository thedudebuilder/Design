<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ODDBALL | XP & Rewards</title>
    
    <!-- TYPOGRAPHY -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Instrument+Serif:ital@0;1&family=Space+Mono:ital,wght@0,400;0,700;1,400&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">

    <style>
        /* --- DESIGN TOKENS --- */
        :root {
            --bg: #030303;
            --panel: #0A0A0A;
            --border: #222;
            --acid: #D4FF00;
            --acid-glow: rgba(212, 255, 0, 0.4);
            --acid-dim: rgba(212, 255, 0, 0.05);
            
            --text-main: #E1E1E1;
            --text-muted: #666;
            
            --font-serif: 'Instrument Serif', serif;
            --font-mono: 'Space Mono', monospace;
            --font-ui: 'Inter', sans-serif;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            background-color: var(--bg);
            color: var(--text-main);
            font-family: var(--font-ui);
            font-size: 13px;
            overflow-x: hidden;
            /* Cyber-Brutalist Scanlines */
            background-image: linear-gradient(rgba(18, 16, 16, 0) 50%, rgba(0, 0, 0, 0.25) 50%), linear-gradient(90deg, rgba(255, 0, 0, 0.06), rgba(0, 255, 0, 0.02), rgba(0, 0, 255, 0.06));
            background-size: 100% 2px, 3px 100%;
        }

        /* --- NAVBAR --- */
        .navbar {
            display: flex; justify-content: space-between; align-items: center;
            padding: 0 30px; height: 60px; border-bottom: 1px solid var(--border);
            background: rgba(3,3,3,0.9); position: sticky; top: 0; z-index: 100;
            backdrop-filter: blur(10px);
        }
        
        .brand { font-family: var(--font-serif); font-size: 1.2rem; font-style: italic; letter-spacing: -0.5px; margin-right: 30px; }
        .nav-links { display: flex; gap: 20px; }
        .nav-item { color: var(--text-muted); text-decoration: none; font-weight: 500; transition: 0.2s; font-family: var(--font-mono); text-transform: uppercase; font-size: 11px; letter-spacing: 1px; }
        .nav-item:hover, .nav-item.active { color: var(--acid); text-shadow: 0 0 10px var(--acid-glow); }
        .nav-right { display: flex; gap: 15px; align-items: center; font-family: var(--font-mono); }
        .cash-display { color: var(--acid); text-shadow: 0 0 8px var(--acid-glow); }
        .user-avatar { width: 24px; height: 24px; background: #222; border-radius: 50%; border: 1px solid var(--border); }

        /* --- PAGE LAYOUT --- */
        .xp-container {
            max-width: 1200px;
            margin: 40px auto;
            padding: 0 20px;
        }

        /* --- HEADER UPDATED TO MATCH SCREENSHOT --- */
        .page-header {
            margin-bottom: 40px;
            border-bottom: 1px solid var(--border);
            padding-bottom: 25px;
            display: flex; justify-content: space-between; align-items: flex-end;
            position: relative;
        }
        .page-header::after {
            content: '+'; position: absolute; bottom: -8px; right: 0; color: var(--acid);
        }

        .ph-title h1 { 
            font-family: var(--font-serif); font-size: 3.5rem; line-height: 0.9; font-weight: 400; 
            background: linear-gradient(180deg, #fff, #888); -webkit-background-clip: text; color: transparent;
        }
        .ph-subtitle { font-family: var(--font-mono); color: var(--text-muted); margin-top: 10px; text-transform: uppercase; letter-spacing: 1px; font-size: 11px; }

        /* New Right Side Season Styling */
        .season-info {
            text-align: right;
        }
        .season-label {
            font-family: var(--font-mono); font-size: 11px; color: #555; 
            text-transform: uppercase; letter-spacing: 1px; margin-bottom: 4px;
        }
        .season-name {
            font-family: var(--font-serif); font-size: 2.2rem; color: #fff;
            line-height: 1; font-weight: 400;
        }

        .dashboard-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 40px;
        }

        /* --- CARDS --- */
        .xp-card {
            background: var(--panel);
            border: 1px solid var(--border);
            padding: 30px;
            margin-bottom: 30px;
            position: relative;
            transition: all 0.3s cubic-bezier(0.25, 1, 0.5, 1);
            overflow: hidden;
        }
        .xp-card:hover {
            border-color: var(--acid);
            box-shadow: 0 0 20px rgba(212, 255, 0, 0.05);
            transform: translateY(-2px);
        }

        /* Tech Corners */
        .xp-card::before {
            content: ''; position: absolute; top: 0; left: 0; width: 10px; height: 10px;
            border-top: 1px solid var(--text-muted); border-left: 1px solid var(--text-muted);
            transition: 0.3s;
        }
        .xp-card::after {
            content: ''; position: absolute; bottom: 0; right: 0; width: 10px; height: 10px;
            border-bottom: 1px solid var(--text-muted); border-right: 1px solid var(--text-muted);
            transition: 0.3s;
        }
        .xp-card:hover::before, .xp-card:hover::after { border-color: var(--acid); width: 100%; height: 100%; opacity: 0.1; pointer-events: none; }

        .card-label {
            font-family: var(--font-mono); font-size: 10px; text-transform: uppercase; letter-spacing: 2px;
            color: var(--text-muted); margin-bottom: 20px; display: flex; justify-content: space-between;
        }
        .card-label::after { content: '///'; color: var(--border); }

        /* --- XP & TIER DISPLAY UPDATED --- */
        .xp-big-display {
            display: flex; align-items: flex-end; gap: 20px; margin-bottom: 25px;
        }
        .xp-val { 
            font-family: var(--font-serif); font-size: 5.5rem; line-height: 0.8; 
            color: var(--acid); 
            text-shadow: 0 0 20px var(--acid-glow);
        }
        
        /* Layout for Unit + Rank Badge */
        .xp-meta-stack {
            display: flex; flex-direction: column; gap: 8px; padding-bottom: 6px;
        }
        .xp-unit { font-family: var(--font-mono); font-size: 1rem; color: #fff; font-style: italic; opacity: 0.5; }
        
        .tier-badge {
            font-family: var(--font-mono); font-size: 11px; text-transform: uppercase; letter-spacing: 0.5px;
            background: rgba(255,255,255,0.05); padding: 4px 8px; border: 1px solid var(--border);
            color: var(--text-main);
        }
        .tier-name { color: var(--acid); font-weight: 700; text-shadow: 0 0 5px var(--acid-glow); }

        /* Striped Progress Bar */
        .progress-track { 
            width: 100%; height: 12px; background: #111; border: 1px solid var(--border); 
            position: relative; margin-bottom: 12px; overflow: hidden;
        }
        .progress-fill { 
            height: 100%; width: 65%; 
            background: repeating-linear-gradient(
                45deg,
                var(--acid),
                var(--acid) 10px,
                #bfff00 10px,
                #bfff00 20px
            );
            box-shadow: 0 0 15px var(--acid-glow);
        }
        .progress-meta { display: flex; justify-content: space-between; font-family: var(--font-mono); font-size: 10px; color: var(--text-main); }

        /* --- REFERRAL SECTION --- */
        .ref-input-group { display: flex; flex-direction: column; gap: 20px; }
        .ref-row { display: flex; gap: 0px; border: 1px solid var(--border); }

        .ref-field-container { flex: 1; position: relative; }
        
        .ref-input {
            width: 100%; background: #000; border: none; color: #fff;
            padding: 15px; font-family: var(--font-mono); font-size: 14px; outline: none;
            text-transform: uppercase; letter-spacing: 1px;
        }
        .ref-input::placeholder { color: #333; }
        
        .btn-action {
            padding: 0 25px; background: #111; border: none; border-left: 1px solid var(--border); color: var(--text-muted);
            font-family: var(--font-mono); cursor: pointer; text-transform: uppercase; font-size: 10px;
            transition: 0.2s; font-weight: 700; letter-spacing: 1px;
        }
        .btn-action:hover { background: var(--acid); color: #000; text-shadow: none; box-shadow: 0 0 15px var(--acid-glow); }
        
        /* --- GUIDELINES LIST --- */
        .guide-list { list-style: none; }
        .guide-item {
            display: flex; justify-content: space-between; align-items: center; padding: 15px 0;
            border-bottom: 1px solid #1a1a1a;
            font-family: var(--font-ui); font-size: 13px; color: #ccc;
            transition: 0.2s;
        }
        .guide-item:hover { color: #fff; padding-left: 5px; }
        .guide-item:last-child { border-bottom: none; }
        .xp-badge {
            background: #111; color: var(--acid); padding: 4px 8px; 
            border: 1px solid var(--border); font-family: var(--font-mono); font-size: 10px;
        }

        /* --- SOCIAL CONNECT --- */
        .social-btn {
            width: 100%; display: flex; justify-content: space-between; align-items: center;
            background: #000; border: 1px solid var(--border); padding: 20px;
            cursor: pointer; transition: 0.2s; position: relative; overflow: hidden;
        }
        .social-btn::before {
            content: ''; position: absolute; top: 0; left: 0; width: 4px; height: 100%; background: var(--acid);
            transform: scaleY(0); transition: 0.2s; transform-origin: bottom;
        }
        .social-btn:hover::before { transform: scaleY(1); }
        .social-btn:hover { border-color: var(--text-muted); background: #0a0a0a; }
        .x-icon { width: 18px; height: 18px; fill: #fff; }

        /* --- MULTIPLIERS --- */
        .multi-grid { display: grid; gap: 15px; }
        .multi-item {
            display: flex; justify-content: space-between; align-items: center;
            padding: 15px; border: 1px solid var(--border); background: #050505;
            position: relative;
        }
        .multi-item.active { border-color: var(--acid); box-shadow: inset 0 0 20px rgba(212, 255, 0, 0.05); }
        .multi-item.active::after {
            content: ''; position: absolute; top: 50%; right: 15px; transform: translateY(-50%);
            width: 6px; height: 6px; background: var(--acid); border-radius: 50%; box-shadow: 0 0 10px var(--acid);
        }
        
        .multi-item.locked { opacity: 0.5; border-style: dashed; }
        .m-val { color: var(--acid); font-family: var(--font-mono); font-weight: 700; margin-right: 15px; }

        /* --- GIF SECTION --- */
        .gif-container {
            width: 100%; height: 220px; overflow: hidden;
            border: 1px solid var(--border); position: relative;
            filter: contrast(1.2) brightness(0.9);
        }
        .gif-container::after {
            content: " "; display: block; position: absolute; top: 0; left: 0; bottom: 0; right: 0;
            background: linear-gradient(rgba(18, 16, 16, 0) 50%, rgba(0, 0, 0, 0.25) 50%), linear-gradient(90deg, rgba(255, 0, 0, 0.06), rgba(0, 255, 0, 0.02), rgba(0, 0, 255, 0.06));
            z-index: 2; background-size: 100% 2px, 3px 100%; pointer-events: none;
        }
        .gif-container img {
            width: 100%; height: 100%; object-fit: cover; opacity: 0.8;
            transition: 0.5s;
        }
        .xp-card:hover .gif-container img { opacity: 1; transform: scale(1.02); }
        
        .gif-caption {
            position: absolute; bottom: 15px; left: 15px; z-index: 3;
            background: var(--acid); color: #000; padding: 4px 8px;
            font-family: var(--font-mono); font-size: 10px; text-transform: uppercase; font-weight: 700;
        }

    </style>
</head>
<body>

    <!-- NAVBAR -->
    <nav class="navbar">
        <div style="display: flex; align-items: center;">
            <div class="brand">ODDBALL.</div>
            <div class="nav-links">
                <a href="#" class="nav-item">Markets</a>
                <a href="#" class="nav-item">Activity</a>
                <a href="#" class="nav-item active">XP Program</a>
            </div>
        </div>
        <div class="nav-right">
            <span class="cash-display">$20.38</span>
            <div class="user-avatar"></div>
        </div>
    </nav>

    <!-- MAIN CONTAINER -->
    <div class="xp-container">

        <!-- HEADER -->
        <div class="page-header">
            <!-- Left Side Title -->
            <div class="ph-title">
                <h1>Loyalty <span style="font-family: var(--font-serif); font-style: italic; color: var(--acid);">&</span><br>Rewards</h1>
                <div class="ph-subtitle">System Status: Online</div>
            </div>
            
            <!-- Right Side Season (Matches Image) -->
            <div class="season-info">
                <div class="season-label">CURRENT SEASON</div>
                <div class="season-name">Season 01: Genesis</div>
            </div>
        </div>

        <div class="dashboard-grid">

            <!-- LEFT COL -->
            <div>
                <!-- XP STATUS CARD -->
                <div class="xp-card">
                    <span class="card-label">Your Balance</span>
                    
                    <div class="xp-big-display">
                        <div class="xp-val">12,450</div>
                        
                        <!-- Tier Badge Integrated Here -->
                        <div class="xp-meta-stack">
                            <span class="xp-unit">XP TOTAL</span>
                            <div class="tier-badge">
                                Rank: <span class="tier-name">ORACLE</span>
                            </div>
                        </div>
                    </div>
                    
                    <div class="progress-track">
                        <div class="progress-fill"></div>
                    </div>
                    <div class="progress-meta">
                        <span>Progress to Level 5</span>
                        <span style="color: var(--acid)">Next: 15,000 XP</span>
                    </div>
                </div>

                <!-- REFERRAL CARD -->
                <div class="xp-card">
                    <span class="card-label">Referral Uplink</span>
                    <p style="color: #888; margin-bottom: 25px; font-size: 12px; line-height: 1.6; font-family: var(--font-mono);">
                        > Invite operators to Oddball.<br>
                        > Receive 10% of fees as XP.<br>
                        > Recruits receive starter bonus.
                    </p>

                    <div class="ref-input-group">
                        <div class="ref-row">
                            <div class="ref-field-container">
                                <input type="text" value="ALAMEDA_GHOST" class="ref-input">
                            </div>
                            <button class="btn-action">Edit Alias</button>
                        </div>
                        <div class="ref-row">
                            <div class="ref-field-container">
                                <input type="text" value="oddball.xyz/ref/ALAMEDA_GHOST" class="ref-input" readonly style="color: var(--text-muted);">
                            </div>
                            <button class="btn-action" style="background: var(--acid); color: #000;">Copy Link</button>
                        </div>
                    </div>
                </div>

                <!-- SOCIAL CONNECT -->
                <div class="xp-card" style="padding: 0; border: none; background: transparent;">
                    <div class="social-btn">
                        <div style="display: flex; gap: 15px; align-items: center;">
                            <svg class="x-icon" viewBox="0 0 24 24"><path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"></path></svg>
                            <span style="font-weight: 600; font-family: var(--font-mono); letter-spacing: -0.5px;">LINK_X_ACCOUNT</span>
                        </div>
                        <div class="xp-badge" style="border-color: var(--acid);">+500 XP</div>
                    </div>
                </div>
            </div>

            <!-- RIGHT COL -->
            <div>

                <!-- EARNING GUIDELINES -->
                <div class="xp-card">
                    <span class="card-label">Bounties</span>
                    <ul class="guide-list">
                        <li class="guide-item">
                            <span>Place a trade > $100</span>
                            <span class="xp-badge">+50 XP</span>
                        </li>
                        <li class="guide-item">
                            <span>Win a prediction market</span>
                            <span class="xp-badge">+100 XP</span>
                        </li>
                        <li class="guide-item">
                            <span>Daily Login Streak (7 Days)</span>
                            <span class="xp-badge">+250 XP</span>
                        </li>
                        <li class="guide-item">
                            <span>Refer a verified user</span>
                            <span class="xp-badge">+500 XP</span>
                        </li>
                        <li class="guide-item">
                            <span style="color: var(--text-muted)">Provide Liquidity (Coming Soon)</span>
                            <span class="xp-badge" style="border-style: dashed; opacity: 0.5;">LOCKED</span>
                        </li>
                    </ul>
                </div>

                <!-- MULTIPLIERS -->
                <div class="xp-card">
                    <span class="card-label">Multipliers</span>
                    <div class="multi-grid">
                        <div class="multi-item active">
                            <span style="font-size: 11px; font-family: var(--font-mono); text-transform: uppercase;">Held 100+ USDC</span>
                            <span class="m-val">+5%</span>
                        </div>
                        <div class="multi-item active">
                            <span style="font-size: 11px; font-family: var(--font-mono); text-transform: uppercase;">Rank: Oracle</span>
                            <span class="m-val">+10%</span>
                        </div>
                        <div class="multi-item locked">
                            <span style="font-size: 11px; font-family: var(--font-mono); text-transform: uppercase;">Genesis NFT Holder</span>
                            <span class="m-val" style="color: var(--text-muted)">+25%</span>
                        </div>
                    </div>
                </div>

                <!-- FUNNY GIF -->
                <div class="xp-card" style="padding: 0; border: none; background: transparent;">
                   <div class="gif-container">
                       <img src="https://media.giphy.com/media/v1.Y2lkPTc5MGI3NjExbXN5aG16Ynd6Y3Z6aHZ5aGZ5aGZ5aGZ5aGZ5aGZ5aGZ5aGZ5aCZlcD12MV9pbnRlcm5hbF9naWZfYnlfaWQmY3Q9Zw/LdOyjZ7io5Msw/giphy.gif" alt="Funny trading gif">
                       <div class="gif-caption">POV: You just hit Level 5</div>
                   </div>
                </div>

            </div>

        </div>

    </div>

</body>
</html>
