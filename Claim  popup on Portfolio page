<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ODDBALL | Payout Event</title>
    
    <!-- FONTS -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Instrument+Serif:ital@0;1&family=Space+Mono:ital,wght@0,400;0,700;1,400&family=Inter:wght@400;600;800&display=swap" rel="stylesheet">

    <style>
        :root {
            --bg: #050505;
            --modal-bg: #0A0A0A;
            --acid: #D4FF00;
            --acid-dim: rgba(212, 255, 0, 0.15);
            --text-main: #EAEAEA;
            --text-muted: #888;
            
            --font-serif: 'Instrument Serif', serif;
            --font-mono: 'Space Mono', monospace;
            --font-ui: 'Inter', sans-serif;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            background-color: var(--bg);
            color: var(--text-main);
            font-family: var(--font-ui);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            
            /* Subtle background grid for context */
            background-image: 
                linear-gradient(rgba(255, 255, 255, 0.03) 1px, transparent 1px),
                linear-gradient(90deg, rgba(255, 255, 255, 0.03) 1px, transparent 1px);
            background-size: 40px 40px;
        }

        /* --- THE MODAL --- */
        .win-modal {
            background: var(--modal-bg);
            width: 420px;
            border: 1px solid var(--acid);
            box-shadow: 0 0 100px rgba(212, 255, 0, 0.15);
            border-radius: 4px;
            padding: 40px 30px;
            position: relative;
            text-align: center;
            
            /* CRT Scanline Overlay effect */
            background-image: 
                linear-gradient(rgba(18, 16, 16, 0) 50%, rgba(0, 0, 0, 0.25) 50%), 
                linear-gradient(90deg, rgba(255, 0, 0, 0.06), rgba(0, 255, 0, 0.02), rgba(0, 0, 255, 0.06));
            background-size: 100% 2px, 3px 100%;
        }

        /* Close Button */
        .close-modal {
            position: absolute; top: 15px; right: 15px;
            background: transparent; border: none; color: var(--text-muted);
            font-family: var(--font-mono); font-size: 14px; cursor: pointer;
            transition: 0.2s;
        }
        .close-modal:hover { color: #fff; }

        /* Glowing Icon */
        .icon-wrapper {
            width: 80px; height: 80px; margin: 0 auto 20px;
            border-radius: 50%;
            background: var(--acid-dim);
            border: 1px solid var(--acid);
            display: grid; place-items: center;
            /* Pulsing Animation */
            box-shadow: 0 0 30px rgba(212, 255, 0, 0.2);
            animation: pulse 2s infinite;
        }

        @keyframes pulse { 
            0% {box-shadow: 0 0 0 0 rgba(212,255,0, 0.4);} 
            70% {box-shadow: 0 0 0 20px rgba(212,255,0, 0);} 
            100% {box-shadow: 0 0 0 0 rgba(212,255,0, 0);} 
        }

        /* Typography */
        .win-amount {
            font-family: var(--font-serif); font-size: 4rem; line-height: 1;
            color: #fff; margin-bottom: 5px; letter-spacing: -1px;
        }
        
        .win-sub {
            font-family: var(--font-mono); font-size: 11px; color: var(--acid);
            text-transform: uppercase; letter-spacing: 2px; margin-bottom: 35px;
        }

        /* Market Recap Box */
        .market-recap {
            background: #111; border: 1px solid #333;
            padding: 15px; border-radius: 4px;
            display: flex; align-items: center; gap: 15px;
            text-align: left; margin-bottom: 30px;
        }
        
        .recap-thumb { 
            width: 45px; height: 45px; border-radius: 4px; 
            object-fit: cover; border: 1px solid #444; 
        }
        
        .recap-text { flex: 1; }
        
        .recap-q { 
            font-family: var(--font-ui); font-size: 13px; font-weight: 600; 
            color: #fff; display: block; margin-bottom: 4px; line-height: 1.3;
        }
        
        .recap-math { 
            font-family: var(--font-mono); font-size: 10px; color: var(--text-muted); 
        }

        /* Buttons */
        .modal-actions { display: flex; flex-direction: column; gap: 12px; }
        
        .btn-confirm-claim {
            width: 100%; padding: 16px; 
            background: var(--acid); border: none;
            color: #000; font-family: var(--font-mono); font-weight: 700; font-size: 12px;
            text-transform: uppercase; cursor: pointer; letter-spacing: 1px;
            /* Cyber Angled Corner */
            clip-path: polygon(0 0, 100% 0, 100% 85%, 97% 100%, 0 100%);
            transition: 0.2s;
        }
        .btn-confirm-claim:hover { opacity: 0.9; transform: translateY(-1px); }
        
        .btn-modal-share {
            width: 100%; padding: 14px; background: transparent; border: 1px solid #333;
            color: var(--text-main); font-family: var(--font-mono); font-size: 11px;
            text-transform: uppercase; cursor: pointer; transition: 0.2s;
        }
        .btn-modal-share:hover { border-color: var(--text-main); }

    </style>
</head>
<body>

    <!-- THE POPUP -->
    <div class="win-modal">
        <button class="close-modal">X</button>
        
        <!-- Animated Icon -->
        <div class="icon-wrapper">
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="var(--acid)" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
                <polyline points="20 6 9 17 4 12"></polyline>
            </svg>
        </div>

        <!-- Main Text -->
        <h2 class="win-amount">$20.37</h2>
        <div class="win-sub">Payout Available</div>

        <!-- Context -->
        <div class="market-recap">
            <img src="https://images.unsplash.com/photo-1614728853970-c8f2b42332b0?q=80&w=200&auto=format&fit=crop" class="recap-thumb" alt="Market">
            <div class="recap-text">
                <span class="recap-q">Will alien life be confirmed by government officials in 2024?</span>
                <span class="recap-math">Bet $19.78 • Won $20.37 (+3.01%)</span>
            </div>
        </div>

        <!-- Actions -->
        <div class="modal-actions">
            <button class="btn-confirm-claim">Execute Claim</button>
            <button class="btn-modal-share">Share on X</button>
        </div>
    </div>

</body>
</html>
