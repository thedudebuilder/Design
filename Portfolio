<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ODDBALL | Portfolio</title>
    
    <!-- FONTS -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Instrument+Serif:ital@0;1&family=Space+Mono:ital,wght@0,400;0,700;1,400&family=Inter:wght@400;600;800&display=swap" rel="stylesheet">

    <style>
        :root {
            --bg: #050505;
            --panel: #0A0A0A;
            --border: #222;
            --acid: #D4FF00;
            --acid-dim: rgba(212, 255, 0, 0.1);
            --cyan: #00F0FF;
            --magenta: #FF0055;
            --text-main: #EAEAEA;
            --text-muted: #888;
            
            --font-serif: 'Instrument Serif', serif;
            --font-mono: 'Space Mono', monospace;
            --font-ui: 'Inter', sans-serif;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        body {
            background-color: var(--bg);
            color: var(--text-main);
            font-family: var(--font-ui);
            min-height: 100vh;
            
            /* GRID BACKGROUND */
            background-image: 
                linear-gradient(rgba(255, 255, 255, 0.03) 1px, transparent 1px),
                linear-gradient(90deg, rgba(255, 255, 255, 0.03) 1px, transparent 1px);
            background-size: 40px 40px;
            background-position: center top;
        }

        /* --- NAVBAR --- */
        .navbar {
            position: fixed; top: 0; left: 0; width: 100%; height: 60px;
            display: flex; justify-content: space-between; align-items: center;
            padding: 0 40px; border-bottom: 1px solid var(--border);
            background: rgba(5,5,5,0.95); z-index: 1000; backdrop-filter: blur(10px);
        }
        .brand { font-family: var(--font-serif); font-size: 1.5rem; font-style: italic; letter-spacing: -1px; }
        .nav-center { display: flex; gap: 30px; }
        .nav-link { font-family: var(--font-mono); font-size: 12px; color: var(--text-muted); cursor: pointer; text-transform: uppercase; text-decoration: none;}
        .nav-link:hover { color: var(--text-main); }
        .nav-right { display: flex; align-items: center; gap: 20px; }
        .balance { font-family: var(--font-mono); font-size: 12px; color: var(--acid); }
        .avatar-circle { width: 30px; height: 30px; background: #222; border-radius: 50%; border: 1px solid var(--border); }

        /* --- LAYOUT --- */
        .main-container {
            max-width: 1200px; margin: 0 auto;
            padding: 100px 20px 60px 20px;
        }

        /* --- TOP DASHBOARD GRID --- */
        .dashboard-grid {
            display: grid;
            grid-template-columns: 1fr 2fr;
            gap: 20px;
            margin-bottom: 30px;
        }

        /* CARD STYLES */
        .panel-card {
            background: var(--panel); border: 1px solid var(--border);
            padding: 25px; border-radius: 4px; position: relative; overflow: hidden;
        }

        /* PORTFOLIO VALUE (Left) */
        .portfolio-header { display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 10px; }
        .p-label { font-family: var(--font-mono); font-size: 11px; color: var(--text-muted); text-transform: uppercase; letter-spacing: 1px; }
        .cash-badge { background: #151515; border: 1px solid #333; padding: 4px 8px; font-family: var(--font-mono); font-size: 10px; border-radius: 4px; display: flex; align-items: center; gap: 6px; }
        .cash-dot { width: 6px; height: 6px; background: var(--acid); border-radius: 50%; box-shadow: 0 0 5px var(--acid); }
        
        .total-value {
            font-family: var(--font-serif); font-size: 4rem; line-height: 1; color: #fff; margin-bottom: 10px;
        }
        .pnl-text { font-family: var(--font-mono); font-size: 12px; color: var(--acid); }
        
        .btn-withdraw {
            width: 100%; margin-top: 30px; padding: 12px;
            background: transparent; border: 1px solid var(--border);
            color: var(--text-muted); font-family: var(--font-mono); font-size: 11px;
            text-transform: uppercase; cursor: pointer; transition: 0.2s;
        }
        .btn-withdraw:hover { border-color: var(--text-main); color: var(--text-main); background: #151515; }

        /* PNL CHART (Right) */
        .chart-header { display: flex; justify-content: space-between; margin-bottom: 20px; }
        .pnl-loss { font-family: var(--font-serif); font-size: 2.5rem; color: #fff; display: flex; align-items: center; gap: 10px; }
        .info-icon { font-size: 14px; color: var(--text-muted); border: 1px solid #333; border-radius: 50%; width: 20px; height: 20px; display: grid; place-items: center; cursor: pointer; font-family: var(--font-mono);}
        
        .time-selectors { display: flex; gap: 5px; }
        .t-btn { background: transparent; border: 1px solid transparent; color: var(--text-muted); font-family: var(--font-mono); font-size: 10px; padding: 4px 8px; cursor: pointer; }
        .t-btn.active { background: #222; color: #fff; border-color: #333; border-radius: 4px; }

        .chart-viz {
            width: 100%; height: 120px; position: relative;
        }
        .scanlines {
            position: absolute; inset: 0; pointer-events: none;
            background: linear-gradient(to bottom, rgba(255,255,255,0), rgba(255,255,255,0) 50%, rgba(0,0,0,0.2) 50%, rgba(0,0,0,0.2));
            background-size: 100% 4px; z-index: 2; opacity: 0.5;
        }

        /* --- CLAIM BANNER (The Action Item) --- */
        .claim-banner {
            background: linear-gradient(90deg, #0f0f0f 0%, #1a1a1a 100%);
            border: 1px solid var(--border); border-left: 4px solid var(--acid);
            padding: 20px 30px; margin-bottom: 40px;
            display: flex; justify-content: space-between; align-items: center;
            position: relative; overflow: hidden;
        }
        /* Dither overlay for style */
        .claim-banner::before {
            content:''; position: absolute; inset: 0; opacity: 0.05; pointer-events: none;
            background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.8' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='1'/%3E%3C/svg%3E");
        }

        .claim-left { display: flex; gap: 20px; align-items: center; z-index: 2; }
        .claim-img { width: 50px; height: 50px; background: #333; border: 1px solid #555; border-radius: 4px; object-fit: cover; }
        .claim-info h3 { font-family: var(--font-serif); font-size: 1.5rem; font-weight: 400; margin-bottom: 5px; color: #fff; }
        .claim-meta { font-family: var(--font-mono); font-size: 11px; color: var(--text-muted); display: flex; gap: 15px; }
        .meta-highlight { color: var(--acid); }

        .btn-claim {
            background: var(--acid); color: #000; border: none;
            font-family: var(--font-mono); font-weight: 700; font-size: 12px;
            padding: 12px 30px; text-transform: uppercase; cursor: pointer;
            box-shadow: 4px 4px 0px #000; transition: transform 0.1s, box-shadow 0.1s;
            z-index: 2;
        }
        .btn-claim:hover { transform: translate(-2px, -2px); box-shadow: 6px 6px 0px #000; }
        .btn-claim:active { transform: translate(0, 0); box-shadow: 0 0 0 #000; }

        /* --- POSITIONS SECTION --- */
        .positions-header {
            border-bottom: 1px solid var(--border); padding-bottom: 0; margin-bottom: 20px;
            display: flex; gap: 30px;
        }
        .p-tab {
            padding: 15px 0; font-family: var(--font-mono); font-size: 12px; 
            color: var(--text-muted); cursor: pointer; border-bottom: 2px solid transparent;
        }
        .p-tab.active { color: var(--text-main); border-color: var(--text-main); }

        .table-controls {
            display: flex; justify-content: space-between; margin-bottom: 20px;
        }
        .search-positions {
            background: #000; border: 1px solid var(--border); color: #fff; padding: 10px 15px;
            font-family: var(--font-mono); font-size: 12px; width: 300px; outline: none;
        }
        .search-positions:focus { border-color: var(--text-muted); }
        .val-toggle {
            background: #111; border: 1px solid var(--border); color: #fff; padding: 8px 15px;
            font-family: var(--font-mono); font-size: 11px; cursor: pointer; display: flex; gap: 8px; align-items: center;
        }

        /* TABLE */
        .pos-table { width: 100%; border-collapse: collapse; }
        .pos-table th {
            text-align: left; font-family: var(--font-mono); font-size: 10px; color: var(--text-muted);
            text-transform: uppercase; padding: 15px; border-bottom: 1px solid var(--border);
        }
        .pos-table td {
            padding: 20px 15px; border-bottom: 1px solid #111; color: #fff; font-family: var(--font-ui); font-size: 13px;
        }
        .pos-row:hover { background: #0E0E0E; }

        .market-cell { display: flex; align-items: center; gap: 15px; }
        .m-thumb { width: 40px; height: 40px; border-radius: 4px; object-fit: cover; border: 1px solid #333; }
        .m-name { font-weight: 600; display: block; margin-bottom: 4px; }
        .m-detail { font-family: var(--font-mono); font-size: 10px; color: var(--magenta); display: flex; gap: 6px; }
        
        .num-mono { font-family: var(--font-mono); font-size: 12px; }
        .val-green { color: var(--acid); }
        .val-red { color: var(--magenta); }

        .btn-sell {
            background: transparent; border: 1px solid var(--acid); color: var(--acid);
            font-family: var(--font-mono); font-size: 10px; padding: 8px 16px; 
            text-transform: uppercase; cursor: pointer; transition: 0.2s;
        }
        .btn-sell:hover { background: var(--acid); color: #000; }
        
        .btn-share {
             background: #111; border: 1px solid #333; color: #fff; width: 32px; height: 32px;
             display: inline-flex; align-items: center; justify-content: center; cursor: pointer;
             margin-left: 5px;
        }
        .btn-share:hover { border-color: #666; }

        @media (max-width: 800px) {
            .dashboard-grid { grid-template-columns: 1fr; }
            .claim-banner { flex-direction: column; align-items: flex-start; gap: 20px; }
            .btn-claim { width: 100%; }
            .pos-table th:nth-child(3), .pos-table td:nth-child(3) { display: none; } /* Hide Avg Price on mobile */
        }
    </style>
</head>
<body>

    <!-- NAVBAR -->
    <nav class="navbar">
        <div class="brand">ODDBALL.</div>
        <div class="nav-center">
            <a href="#" class="nav-link">Markets</a>
            <a href="#" class="nav-link">Feed</a>
            <a href="#" class="nav-link">Leaderboard</a>
        </div>
        <div class="nav-right">
            <span class="balance">$30.43</span>
            <div class="avatar-circle"></div>
        </div>
    </nav>

    <div class="main-container">

        <!-- 1. DASHBOARD STATS -->
        <div class="dashboard-grid">
            
            <!-- Portfolio Value -->
            <div class="panel-card">
                <div class="portfolio-header">
                    <span class="p-label">Portfolio Value</span>
                    <div class="cash-badge"><div class="cash-dot"></div> $0.01 CASH</div>
                </div>
                <div class="total-value">$30.43</div>
                <div class="pnl-text">+ $0.03 (0.10%) Today</div>
                <button class="btn-withdraw">↑ Withdraw Funds</button>
            </div>

            <!-- PnL Chart -->
            <div class="panel-card">
                <div class="chart-header">
                    <div style="display:flex; flex-direction:column;">
                        <span class="p-label" style="margin-bottom:5px;">Profit / Loss</span>
                        <div class="pnl-loss">-$76.21 <span class="info-icon">i</span></div>
                        <span style="font-family:var(--font-mono); font-size:10px; color:var(--text-muted); margin-top:5px;">Past Month</span>
                    </div>
                    <div class="time-selectors">
                        <button class="t-btn">1D</button>
                        <button class="t-btn">1W</button>
                        <button class="t-btn active">1M</button>
                        <button class="t-btn">ALL</button>
                    </div>
                </div>
                
                <!-- CSS/SVG Chart Visualization -->
                <div class="chart-viz">
                    <div class="scanlines"></div>
                    <svg width="100%" height="100%" viewBox="0 0 600 120" preserveAspectRatio="none">
                         <defs>
                            <linearGradient id="gradLoss" x1="0%" y1="0%" x2="0%" y2="100%">
                                <stop offset="0%" style="stop-color:var(--magenta);stop-opacity:0.1" />
                                <stop offset="100%" style="stop-color:var(--magenta);stop-opacity:0" />
                            </linearGradient>
                        </defs>
                        <!-- Fill -->
                        <path d="M0,20 L20,20 L40,60 L60,90 L80,95 L100,92 L600,92 L600,150 L0,150 Z" fill="url(#gradLoss)" />
                        <!-- Line -->
                        <path d="M0,20 L20,20 L40,60 L60,90 L80,95 L100,92 L600,92" 
                              fill="none" stroke="var(--magenta)" stroke-width="2" vector-effect="non-scaling-stroke"
                              style="filter: drop-shadow(0 0 6px var(--magenta));" />
                    </svg>
                </div>
            </div>
        </div>

        <!-- 2. CLAIM BANNER (The "Win" State) -->
        <div class="claim-banner">
            <div class="claim-left">
                <img src="https://images.unsplash.com/photo-1614728853970-c8f2b42332b0?q=80&w=200&auto=format&fit=crop" class="claim-img" alt="Market Thumb">
                <div class="claim-info">
                    <h3>Market Resolved: YES</h3>
                    <div class="claim-meta">
                        <span>Alien Life Found (2024)</span>
                        <span>Proceeds: <span class="meta-highlight">$20.37</span></span>
                        <span>Return: <span class="meta-highlight">301%</span></span>
                    </div>
                </div>
            </div>
            <button class="btn-claim">Claim Winnings</button>
        </div>

        <!-- 3. POSITIONS / HISTORY -->
        <div class="positions-section">
            <div class="positions-header">
                <div class="p-tab active">Positions</div>
                <div class="p-tab">Open Orders</div>
                <div class="p-tab">History</div>
            </div>

            <div class="table-controls">
                <input type="text" class="search-positions" placeholder="Search positions...">
                <button class="val-toggle">⇄ Current Value</button>
            </div>

            <table class="pos-table">
                <thead>
                    <tr>
                        <th width="40%">Market</th>
                        <th width="15%">Avg > Current</th>
                        <th width="10%">Bet</th>
                        <th width="10%">To Win</th>
                        <th width="10%">Value (PnL)</th>
                        <th width="15%" style="text-align:right;">Action</th>
                    </tr>
                </thead>
                <tbody>
                    <!-- Row 1 -->
                    <tr class="pos-row">
                        <td>
                            <div class="market-cell">
                                <img src="https://images.unsplash.com/photo-1556742049-0cfed4f7a07d?q=80&w=200&auto=format&fit=crop" class="m-thumb">
                                <div>
                                    <span class="m-name">Fed decreases interest rates by 50+ bps?</span>
                                    <span class="m-detail">NO 98¢ • 10.2 Shares</span>
                                </div>
                            </div>
                        </td>
                        <td class="num-mono">98¢ > 98¢</td>
                        <td class="num-mono">$10.00</td>
                        <td class="num-mono">$10.20</td>
                        <td>
                            <div class="num-mono">$10.05</div>
                            <div class="num-mono val-green" style="font-size:10px;">+$0.05 (0.46%)</div>
                        </td>
                        <td style="text-align:right;">
                            <button class="btn-sell">Sell</button>
                            <button class="btn-share">↗</button>
                        </td>
                    </tr>
                    
                     <!-- Row 2 (Filler for visual) -->
                     <tr class="pos-row">
                        <td>
                            <div class="market-cell">
                                <img src="https://images.unsplash.com/photo-1518546305927-5a555bb7020d?q=80&w=200&auto=format&fit=crop" class="m-thumb">
                                <div>
                                    <span class="m-name">Bitcoin > $100k in Q1?</span>
                                    <span class="m-detail" style="color:var(--cyan);">YES 42¢ • 50 Shares</span>
                                </div>
                            </div>
                        </td>
                        <td class="num-mono">35¢ > 42¢</td>
                        <td class="num-mono">$17.50</td>
                        <td class="num-mono">$50.00</td>
                        <td>
                            <div class="num-mono">$21.00</div>
                            <div class="num-mono val-green" style="font-size:10px;">+$3.50 (20%)</div>
                        </td>
                        <td style="text-align:right;">
                            <button class="btn-sell">Sell</button>
                            <button class="btn-share">↗</button>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>

    </div>

</body>
</html>
